import React from "react";
import {arrayOf, object} from "prop-types";

export default function GridRow({data}){
    return <div>

    </div>
}

GridRow.propTypes = {
    data: arrayOf(object).isRequired
}
